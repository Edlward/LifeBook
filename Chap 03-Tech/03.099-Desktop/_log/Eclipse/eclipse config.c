





//------------------------------------//
0003. font.
prefences -> C/C++ -> Code Style ->
prefences -> C/C++ -> Editor     -> Syntax color.
prefences -> C/C++ -> Editor -> (self) workspace default :Doxygen.

//------------------------------------//
0002. short key.
//ctrl.
//alt.
//ctrl-alt.
//ctrl-shift.
//alt -shift.


F3 				跳转到类、变量的声明

Ctrl+/ 			注释代码 
Alt+/ 			补齐代码 、

//ctrl.
Ctrl+D			删除当前行 					Delete.
Ctrl+Q 			定位到最后编辑的地方 		Quick.
//ctrl-alt.
Ctrl+Alt+↓ 		复制当前行到下一行(复制增加)
Ctrl+Alt+↑ 		复制当前行到上一行(复制增加)
//alt.
Alt+↓ 			当前行和下面一行交互位置(特别实用,可以省去先剪切,再粘贴了)
Alt+↑ 			当前行和上面一行交互位置(同上)
Alt+← 			前一个编辑的页面
Alt+→ 			下一个编辑的页面(当然是针对上面那条来说了)
//ctrl-shift.
Ctrl+Shift+X 	把当前选中的文本全部变为大写
Ctrl+Shift+Y 	把当前选中的文本全部变为小写
Ctrl+Shift+F，  程序代码自动排版 			Format, space (),
Ctrl+Shift+P 	后-定位到对于的匹配符		Pair (譬如{}) 
(从前面定位后面时,光标要在匹配符里面,后面到前面,则反之)
//alt-shift.
Alt+Shift+R 	类-重命名					(Rename.)


//------------------------------------//
0001. project name.
<?xml version="1.0" encoding="UTF-8"?>
<projectDescription>
	<name>SwingDoorPlatform</name> //===// <name>####</name>