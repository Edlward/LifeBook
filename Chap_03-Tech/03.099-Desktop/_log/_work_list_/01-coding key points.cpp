0001. /NOT PUSH. give suggestions. (beer, git branch, code)/.
0002. 

0011. /Not desc, try sign °。 ℃，∠，
0012. 单位( 0.1度, 十分度； 0.01度， 百分度)。
0013. //DOCUMENT; LINT; UNIT-TEST; 测试驱动，把自己当成测试人员，测试软件功能LockVoltageMonitor. use case 使用情形，用例场景。
0014. //Git Rebase: checkout new branch, upstream on old one; Not push new branch, cause it is easy to REBASE locally.
0015. //Fetch -prune； Check web first then push, or fetch first. //delete wrong push.
0015. 快速调试//Fast debug: change struct(switch out)。